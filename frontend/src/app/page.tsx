import { LandingHero } from "@/components/landing/landing-hero";

/**
 * Root route: immersive landing page with particle field,
 * hero section, and feature showcase grid.
 */
export default function RootPage() {
  return <LandingHero />;
}
