"""Legacy manual macro-events smoke script placeholder."""

from __future__ import annotations

import pytest

pytest.skip(
    "Legacy manual macro-events checks are excluded from automated pytest.",
    allow_module_level=True,
)
